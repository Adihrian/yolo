apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: default
spec:
  selector:
    app: yolo-client
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 80    
  type: LoadBalancer
